This directory contains the binary executable images for the six
demo programs along with the 'pshell' UDP/UNIX client program.  On 
a systen wide install only the 'pshell' client program will be copied
to /usr/bin.

